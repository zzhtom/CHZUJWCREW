'use strict';
/**
 * logic
 * @param  {} []
 * @return {}     []
 */
export default class extends think.logic.base {
  /**
   * index action logic
   * @return {} []
   */
  async indexAction() {
    /*let ndata = await this.model('photograph').where({ pid: Number(this.get('picid')) + 1 }).find();
    let pdata = await this.model('photograph').where({ pid: Number(this.get('picid')) - 1 }).find();
    if (think.isEmpty(pdata) || think.isEmpty(pdata)) {
      return false;
    }*/
  }
}